{
  "name": "my-3d-portfolio",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "@types/three": "^0.177.0",
    "three": "^0.158.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^4.4.9"
  }
}
